'use strict'

const express = require('express')

// O reajuste automático de materiais foi retirado temporariamente do escopo.
// Este router permanece vazio apenas para manter o require do server.js estável
// durante a conclusão e validação de Models V2.
const router = express.Router()

module.exports = router
